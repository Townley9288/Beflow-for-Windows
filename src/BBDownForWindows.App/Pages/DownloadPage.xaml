<Page
    x:Class="BBDownForWindows.App.Pages.DownloadPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    DataContext="{x:Bind ViewModel, Mode=OneWay}">
    <Grid x:Name="LayoutGrid" Padding="24" ColumnSpacing="20" RowSpacing="20">
        <Grid.RowDefinitions>
            <RowDefinition x:Name="MainRow" Height="*" />
            <RowDefinition x:Name="NarrowLogRow" Height="0" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition x:Name="ControlColumn" Width="5*" />
            <ColumnDefinition x:Name="LogColumn" Width="7*" />
        </Grid.ColumnDefinitions>
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <VisualState x:Name="Wide"><VisualState.StateTriggers><AdaptiveTrigger MinWindowWidth="1000" /></VisualState.StateTriggers></VisualState>
                <VisualState x:Name="Narrow">
                    <VisualState.StateTriggers><AdaptiveTrigger MinWindowWidth="0" /></VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="ControlColumn.Width" Value="*" />
                        <Setter Target="LogColumn.Width" Value="0" />
                        <Setter Target="MainRow.Height" Value="Auto" />
                        <Setter Target="NarrowLogRow.Height" Value="520" />
                        <Setter Target="LogPanel.(Grid.Column)" Value="0" />
                        <Setter Target="LogPanel.(Grid.Row)" Value="1" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>

        <ScrollViewer x:Name="ControlsScrollViewer" Grid.Row="0" Grid.Column="0" VerticalScrollBarVisibility="Auto">
            <StackPanel Spacing="16">
                <StackPanel>
                    <TextBlock Text="下载" Style="{StaticResource TitleTextBlockStyle}" />
                    <TextBlock Text="下载普通视频、多分P及整季内容" Opacity="0.65" />
                </StackPanel>
                <StackPanel Spacing="12">
                <Border Padding="16" CornerRadius="8" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                    <StackPanel Spacing="12">
                        <TextBlock Text="视频地址" Style="{StaticResource SubtitleTextBlockStyle}" />
                        <TextBox Text="{Binding Url, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" PlaceholderText="输入 B站链接、BV、AV、EP 或 SS 号" />
                        <TextBox Text="{Binding Pages, Mode=TwoWay}" Header="指定分P（可选）" PlaceholderText="例如 1,2、3-5、ALL、LAST" />
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <Button Content="获取信息" Command="{Binding GetInfoCommand}" />
                            <Button Content="开始下载" Command="{Binding DownloadCommand}" Style="{StaticResource AccentButtonStyle}" />
                            <Button Content="整季下载" Command="{Binding SeasonCommand}" />
                            <Button Content="取消" Command="{Binding Console.CancelCommand}" />
                        </StackPanel>
                    </StackPanel>
                </Border>
                <Border Padding="16" CornerRadius="8" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                    <StackPanel Spacing="12">
                        <TextBlock Text="下载选项" Style="{StaticResource SubtitleTextBlockStyle}" />
                        <Grid ColumnSpacing="12" RowSpacing="12">
                            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                            <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
                            <ComboBox Header="视频质量" HorizontalAlignment="Stretch" ItemsSource="{Binding QualityOptions}" SelectedItem="{Binding Quality, Mode=TwoWay}" />
                            <ComboBox Grid.Column="1" Header="编码优先" HorizontalAlignment="Stretch" ItemsSource="{Binding EncodingOptions}" SelectedItem="{Binding Encoding, Mode=TwoWay}" />
                            <ComboBox Grid.Column="2" Header="音频格式" HorizontalAlignment="Stretch" ItemsSource="{Binding AudioCodecOptions}" DisplayMemberPath="Label" SelectedValuePath="Value" SelectedValue="{Binding AudioCodec, Mode=TwoWay}" />
                            <ComboBox Grid.Column="3" Header="下载类型" HorizontalAlignment="Stretch" ItemsSource="{Binding DownloadModeOptions}" SelectedItem="{Binding DownloadModeText, Mode=TwoWay}" />
                            <ComboBox Grid.Row="1" Header="音频码率" HorizontalAlignment="Stretch" ItemsSource="{Binding AudioBitrateOptions}" DisplayMemberPath="Label" SelectedValuePath="Value" SelectedValue="{Binding AudioBitrate, Mode=TwoWay}" />
                            <ComboBox Grid.Row="1" Grid.Column="1" Header="CDN 节点" HorizontalAlignment="Stretch" ItemsSource="{Binding UposHostOptions}" DisplayMemberPath="Label" SelectedValuePath="Value" SelectedValue="{Binding UposHost, Mode=TwoWay}" />
                        </Grid>
                        <StackPanel Orientation="Horizontal" Spacing="18">
                            <CheckBox Content="下载弹幕" IsChecked="{Binding Danmaku, Mode=TwoWay}" />
                            <CheckBox Content="下载字幕" IsChecked="{Binding Subtitle, Mode=TwoWay}" />
                            <CheckBox Content="下载封面" IsChecked="{Binding Cover, Mode=TwoWay}" />
                        </StackPanel>
                    </StackPanel>
                </Border>
                <Border Padding="16" CornerRadius="8" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                    <StackPanel Spacing="12">
                        <TextBlock Text="下载配置" Style="{StaticResource SubtitleTextBlockStyle}" />
                        <Grid ColumnSpacing="8"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                            <TextBox Text="{Binding WorkDirectory, Mode=TwoWay}" Header="下载目录" />
                            <Button Grid.Column="1" Content="浏览…" VerticalAlignment="Bottom" Click="BrowseFolder_Click" />
                        </Grid>
                        <StackPanel Orientation="Horizontal" Spacing="18">
                            <CheckBox Content="启用多线程" IsChecked="{Binding MultiThread, Mode=TwoWay}" />
                            <CheckBox Content="使用 aria2c" IsChecked="{Binding UseAria2c, Mode=TwoWay}" />
                            <CheckBox Content="保存任务日志" IsChecked="{Binding SaveTaskLogs, Mode=TwoWay}" />
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <Button Content="网页登录" Command="{Binding LoginWebCommand}" />
                            <Button Content="TV 登录" Command="{Binding LoginTvCommand}" />
                        </StackPanel>
                        <StackPanel x:Name="QrPanel" Visibility="Collapsed" HorizontalAlignment="Center" Spacing="8">
                            <Image x:Name="QrImage" Width="180" Height="180" Stretch="Uniform" />
                            <TextBlock x:Name="QrStatus" Text="扫码后请在手机端确认" HorizontalAlignment="Center" />
                        </StackPanel>
                    </StackPanel>
                </Border>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>

        <Border x:Name="LogPanel" Grid.Row="0" Grid.Column="1" Padding="16" CornerRadius="8" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
            <Grid RowSpacing="10"><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                    <StackPanel><TextBlock Text="运行日志" Style="{StaticResource SubtitleTextBlockStyle}"/><TextBlock Text="{Binding Console.Status}" Opacity="0.65"/></StackPanel>
                    <Button Grid.Column="1" Content="复制" Margin="0,0,8,0" Click="CopyLogs_Click" />
                    <Button Grid.Column="2" Content="清空" Command="{Binding Console.ClearCommand}" />
                </Grid>
                <TextBox x:Name="LogTextBox" Grid.Row="1" Text="{Binding Console.Logs, Mode=OneWay}" IsReadOnly="True" AcceptsReturn="True" TextWrapping="NoWrap" FontFamily="Consolas" ScrollViewer.VerticalScrollBarVisibility="Auto" ScrollViewer.HorizontalScrollBarVisibility="Auto" TextChanged="LogTextBox_TextChanged" />
            </Grid>
        </Border>
    </Grid>
</Page>
