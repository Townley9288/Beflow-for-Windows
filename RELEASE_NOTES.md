# Beflow for Windows v1.1.1.12

- 修复软件重启后，队列为空或只剩完成、失败、取消记录时，新加入的任务仍需手动继续的问题。有未完成任务或本次运行中手动暂停时，仍需点击“继续”。
- 移除下载队列切换分类 Tab 时任务列表的渐入动画，内容直接显示。
- 修复任务卡片与分类标题左侧不对齐，以及隐藏按钮导致操作按钮间距过大的问题；窄窗口下继续支持自动换行。
- 已完成的视频任务新增“去重命名”入口，自动带入成品目录、标题并勾选本次任务的视频文件，支持普通下载与多音轨封装；沿用现有重命名页的视频同目录规则。
